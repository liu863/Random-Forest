import java.util.*;

public class Table {
    
    int col_count, row_count;
    boolean[] col_type; //ture - string, false - double
    String[] col_name;
    List<List<Object>> data;
    
    public Table(int count, boolean[] type, String[] name) {
        col_count = count;
        col_type = new boolean[col_count];
        col_name = new String[col_count];
        for (int i = 0; i < col_count; i++) {
            col_type[i] = type[i];
            col_name[i] = name[i];
        }
    }
    
    public void addRow(String str) {
    
    }
    
    public void split(int col, Object value) {
    
    }
    
    public void printTable() {
    
    }
}